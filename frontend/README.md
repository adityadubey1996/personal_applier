# Frontend V5 (Phase 0)

This is intentionally a minimal HTML UI served by `v5/backend`.

It provides:

- session start/release
- command submit/stop
- live Steel iframe viewer
- log + audit event stream

No framework bootstrapping yet; this is for first-principles debugging.
